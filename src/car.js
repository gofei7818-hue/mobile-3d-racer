import * as THREE from 'three';
export function makeCar(color){ const g=new THREE.Group(); const body=new THREE.Mesh(new THREE.BoxGeometry(2.2,0.8,4.0),new THREE.MeshStandardMaterial({color,roughness:0.6})); body.position.y=0.55; g.add(body); const cabin=new THREE.Mesh(new THREE.BoxGeometry(2.0,0.7,1.8),new THREE.MeshStandardMaterial({color:0x111827,roughness:0.3})); cabin.position.set(0,0.95,-0.4); g.add(cabin); const wg=new THREE.CylinderGeometry(0.45,0.45,0.35,14), wm=new THREE.MeshStandardMaterial({color:0x0b0f14,roughness:1}); [[-1.1,0.35,-1.3],[1.1,0.35,-1.3],[-1.1,0.35,1.3],[1.1,0.35,1.3]].forEach(([x,y,z])=>{const w=new THREE.Mesh(wg,wm);w.rotation.z=Math.PI/2;w.position.set(x,y,z);g.add(w);}); return g; }
